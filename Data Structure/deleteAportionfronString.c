#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main()
{
    char text,p;
    int l1,l2,k;
    printf("Enter text?\n");
    gets(text);
    printf("Enter a portion of the text you wanna delete?\n");
    gets(p);
    l1=strlen(text);
    l2=strlen(p);

    printf("%d\n",k);
}
